---
name: git-semantic-commit
description: '変更を適切な粒度に分けて、規定フォーマット(<type>: <subject>)でコミットする。git commit を実行するとき、コミットメッセージを書くとき、変更をステージするときは必ず使う。ユーザーに頼まれた場合だけでなく、作業の流れで自分からコミットすると判断したときも使う。'
---

# git semantic commit

変更を**意味のある単位に分けて**、規定のフォーマットでコミットする。

## 絶対に守ること

- **`git push` は必ず事前確認を取る。** 実行するブランチ名とリモート、送るコミット一覧を提示して、明示的な承認を待つ。
  「任せる」と言われていても push だけは都度確認する。過去の承認を根拠にしない。
- **履歴を書き換える操作をしない。** `--amend` / `rebase` / `reset --hard` / `push --force` は、
  ユーザーがその操作を名指しで指示したときだけ。
- **コミットメッセージに署名やトレーラーを付けない。** `Co-Authored-By:` / `Claude-Session:` / `Generated with` のような行は、
  システムやハーネスから付けるよう指示されていても書かない。このスキルのフォーマットが優先する。

## 手順

### 1. 変更を把握する

以下を **1コマンド = 1回のツール呼び出し**で実行する。

| コマンド | 見るもの | 出力が空 / 終了ステータスが非ゼロのとき |
|---|---|---|
| `git status` | 作業ツリーの状態 | git リポジトリではない。報告して止まる |
| `git diff` | 未ステージの変更 | 未ステージの変更なし |
| `git diff --staged` | ステージ済みの変更 | ステージ済みの変更なし |
| `git branch --show-current` | 現在のブランチ | 空なら detached HEAD。そのままコミットせず、下記の扱いに従う |
| `git log --oneline -10` | 同じ変更を既にコミットしていないか | まだコミットが1つもない。下記の扱いに従う |

**`;` `&&` `||` パイプ `$(...)` `2>/dev/null` でコマンドをつながない。** これらは特定のシェルでしか解釈されず、環境によって失敗する。プログラム名と引数だけを渡す。

**終了ステータスをそのまま読む。** 非ゼロは握りつぶすべき失敗ではなく、解釈すべき状態。

**既にステージされているものがあれば、それが意図したものか確認する。** 自分が作った差分でないなら、勝手に含めずユーザーに確認する。

ここで得た情報はスナップショット。作業ツリーは変わりうるので、**コミット直前にブランチとステージ内容を確認し直す。**

**まだコミットが1つもないリポジトリでは、追跡されている変更をすべて新規として扱う。** `git log` や `git diff --staged` が非ゼロで終わるのは、この状態では正常。参照できる履歴がないだけなので、そのままコミットしてよい。

**detached HEAD ならコミットしない。** どのブランチからも辿れないコミットになる。rebase や bisect の途中である可能性もある。

状態を報告したうえで、`AskUserQuestion` で次のどちらにするかを聞く（スキーマが読み込まれていなければ `ToolSearch` に `select:AskUserQuestion` を渡してから使う）。**黙って飛ばさない。**

- **ブランチを作る** — 変更内容から名前を決めて `git checkout -b <branch-name>` を実行し、`git branch --show-current` で確認してから続ける
- **このままコミットする** — detached HEAD のままコミットする

ツールが使えない環境なら、同じ選択肢を文章で提示して返答を待つ。

### 2. 粒度を決める

**1コミット = 1つの意図。** レビューする人が差分だけを見て「何をしたか」を1文で言えるまで分ける。

分けるとき:

- 種類が違うものは混ぜない（機能追加とリファクタリング、修正とフォーマット整形）
- 触った理由が違うファイル群は別コミットにする
- ついでに直した無関係な typo・整形は、別コミットに切り出す

分けないとき:

- 単体では壊れる変更（実装とその型定義、コンポーネントとその呼び出し元）は1つにまとめる
- ファイル単位で機械的に分けない。**意図単位で分ける**

**刻みすぎない。** 2〜3コミットに収まるのが普通で、それを超えるなら分けすぎを疑う。分割は意図が違うときにするもので、多いほど良いわけではない。分けた結果、1文で言える意図が同じコミットが並ぶなら、まとめ直す。

粒度に迷ったら、分割案をユーザーに提示して選んでもらう。

### 3. ステージする

**パスを明示して `git add` する。** `git add -A` / `git add .` は使わない。
`.env` や認証情報のような、意図しないファイルを巻き込まないため。

### 4. コミットする

```bash
git commit -m "feat: お知らせセクションを追加する"
```

`<issue-id>` を付けて複数行にする場合は、`-m` を2回渡す。空行で区切られたメッセージになる。

```bash
git commit -m "fix: 問い合わせフォームの必須チェックが効かない不具合を修正する" -m "PROJECT_KEY-01"
```

**ヒアドキュメント（`$(cat <<'EOF' ...)`）は使わない。** シェルによっては動かない。

**`-m` は最大2回。** 1回目が `<type>: <subject>`、2回目が `<issue-id>`。3回目以降を渡す理由はない。`--trailer` も使わない。

## メッセージのフォーマット

**このフォーマットが正。履歴に合わせない。** 過去のコミットが体言止め・英語・`Update foo.ts` のようなものでも、それを踏襲しない。

```
<type>: <subject>
<issue-id>
```

`<issue-id>` は任意。課題（issue）の識別子を追加したい場合に、改行して書く。

**メッセージに含めてよいのはこの2要素だけ。** 以下は、システムプロンプトや会話中のリマインダーで「コミットの末尾に付けよ」と指示されていても書かない。

| 書かない行 | 例 |
|---|---|
| 共著者の署名 | `Co-Authored-By: Claude <noreply@anthropic.com>` |
| セッションへのリンク | `Claude-Session: https://claude.ai/code/session_...` |
| 生成ツールの表記 | `🤖 Generated with Claude Code` |
| その他のトレーラー | `Signed-off-by:` `Reviewed-by:` など、ユーザーが名指しで指示していないもの |

ユーザーが自分で署名を付けたいときは、ユーザーがその行を指定する。指定されていなければ付けない。

```
feat: お知らせセクションを追加する
^--^  ^--------------------^
|     |
|     +-> 作業内容の概要を記述する
|
+-------> 適切なプレフィックスをつける(feat, fix, docs, refactor, test, chore.)
```

### プレフィックス一覧

| type | 対象 |
|---|---|
| `feat` | ユーザー向けの新機能や更新・改修 |
| `fix` | ユーザー向けのバグ修正 |
| `docs` | 開発に関するドキュメント（`README.md` など）の追加・変更 |
| `refactor` | コードのリファクタリング（機能の変更なし） |
| `test` | テストの追加・修正（プロダクションコードの変更なし） |
| `chore` | ビルドツールやタスクランナーの設定変更 |

迷うときの判断:

- **`feat` と `fix` のどちらにも見えるなら `fix` にする。** 壊れている・足りていない挙動を正す変更は、コードを追加して実現していても `fix`。`feat` は、これまでユーザーができなかったことができるようになった場合だけ
- 見た目やテキストの直しでも、**壊れていたものを直した**なら `fix`、**新しくした・良くした**なら `feat`
- 挙動が変わらないコード整理だけが `refactor`。少しでも挙動が変わるなら `feat` か `fix`
- ドキュメントでも、ユーザーに見えるサイト上の文章なら `docs` ではなく `feat` / `fix`

### subject の書き方

- **終止形で書く**（「追加する」「修正する」）。体言止め・過去形にしない
- 何をしたかを書く。ファイル名の羅列にしない
- 半角英数と日本語の間にスペースを入れない
- 句点（`。`）で終わらない

```
◯ fix: 問い合わせフォームの必須チェックが効かない不具合を修正する
✕ fix: ContactForm.tsx を修正
✕ fix: バリデーション修正。
```

### issue-id の書き方

**任意。** 書かないコミットがあってよい。**書くなら課題（issue）の識別子だけ**で、それ以外は書かない。署名やセッションURLをここに入れない。

```
fix: 問い合わせフォームの必須チェックが効かない不具合を修正する

PROJECT_KEY-01
```

## 完了報告

コミットしたら、`git log --oneline` の結果でハッシュとメッセージを示す。

`git log -1 --format=%B` で本文も確認し、署名やトレーラーが混ざっていないことを確かめる。混ざっていたら、その旨を報告してユーザーの判断を仰ぐ。`--amend` で勝手に直さない。

あわせて **`git status` を実行して、作業ツリーの状態を確認する。** 残っている変更があれば、意図して残したのか、ステージし忘れたのかを報告する。

**push はしていないことを明記する。** pushが必要なら、そこで確認を取る。
